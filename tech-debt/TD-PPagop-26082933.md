---
id: TD-PPagop-26082933
title: "main's TD-PPagop-26082931.md says 'Distinct from TD-PPagop-26082931', pointing at itself where it means TD-PPagop-26082932"
status: open
filed: 2026-08-29
---

**What.** `tech-debt/TD-PPagop-26082931.md` (the watchtower-pre-update 48h-trim item, merged to main in c6b0688) ends with 'Distinct from TD-PPagop-26082931 (reader routing on a corrupt *last* line)'. That sentence was written when the record it means still carried the ID 26082931 on PR #929's branch; the rebase resolving that add/add ID collision refiled the branch's record as `TD-PPagop-26082932`, so the pointer now names the record it sits in and describes the wrong item.

**Why it matters.** Whoever resolves either item has to re-derive the mapping from PR #929's comment thread, which nothing links from either record file. The register's append-only guard (correctly) refuses a body edit to a merged open item, which is why PR #929 could not fix it in place — the Reviewer flagged it only in a PR comment, which is exactly the kind of place a finding is lost.

**Where.** `tech-debt/TD-PPagop-26082931.md` (final paragraph); the correct target is `tech-debt/TD-PPagop-26082932.md` ('An unparseable *last* ledger line sends updater_status to the service-scoped fallback').

**Suggested fix.** Correct the pointer to `TD-PPagop-26082932` whenever the register's policy next permits touching that body (e.g. at that item's own resolution, or if the append-only guard grows a scoped-correction path); until then this record is the durable statement of the mapping, so a resolver of either item need not re-derive it from PR #929's comments.

Noticed while the Approver was judging https://github.com/Poetic-Poems/agent-ops/pull/929.
