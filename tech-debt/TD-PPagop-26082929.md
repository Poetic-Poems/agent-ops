---
id: TD-PPagop-26082929
title: "docs/IMPLEMENTATION-PIPELINE-SPEC.md:11709 still phrases a live counterfactual with the retired 'hourly' cadence"
status: open
filed: 2026-08-29
---

**What.** `docs/IMPLEMENTATION-PIPELINE-SPEC.md` line 11709, describing the
same self-heal behaviour as `scripts/sweep-human-visibility.sh`'s
`confirm_review_requested` (requirement 34a), says a blind re-request would
drop the pull request out of the Implementer's own review-feedback selection
while the human's `CHANGES_REQUESTED` sat unanswered — "PR #205's
silent-starvation failure, reintroduced hourly and fleet-wide." Like the
script comment TD-PPagop-26082314 just fixed (PR #932), this is a
present-tense counterfactual describing what would happen today without the
guard, so "hourly" is a live claim about the cycle cadence — the exact class
PR #707's sweep retired — not a historical narrative or an external-cadence
mention.

**Why it matters.** The spec is as-built (`CLAUDE.md`'s "Requirements state
only what is, never what used to be or what is planned"); this line presents
a retired cadence as part of the pipeline's live identity, the same defect
TD-PPagop-26082314 targeted in the mirrored script comment. PR #707's sweep
and TD-PPagop-26082314's fix both missed it because it lives in the spec, not
in a script.

**Where.** `docs/IMPLEMENTATION-PIPELINE-SPEC.md:11709`.

**Suggested fix.** Reword to cadence-neutral phrasing, matching PR #932's
fix to the mirrored script comment: "reintroduced cycle after cycle and
fleet-wide" (or equivalent), still naming PR #205's failure. Comment/doc-only
change, no behaviour implied.

Noticed while reviewing PR #932 (TD-PPagop-26082314).
